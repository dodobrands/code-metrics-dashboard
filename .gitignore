# Metrics data is never committed — generated at publish time (mcm get → build.py)
# and injected into the Pages artifact only (see .github/workflows/deploy.yml).
raw.json
data/raw.json
data/meta.json
data/charts/
