# Pages are stamped from templates/ per app in apps.json at build time; metrics data
# and share cards are generated then too. None of it is committed — it lives only in
# the Pages artifact (see .github/workflows/deploy.yml and scripts/build-apps.sh).
/index.html
/*/index.html
**/data/
raw.json
node_modules/
bundle.js
og.png

# SwiftPM build artifacts for the data build tool (Tools/DashboardBuild)
.build/
Tools/DashboardBuild/.build/
Package.resolved
