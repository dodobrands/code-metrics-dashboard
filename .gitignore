# Metrics data is never committed — fetched fresh at publish time and injected
# into the Pages artifact only (see .github/workflows/deploy.yml).
raw.json
data/raw.json
metrics.json
data/metrics.json
