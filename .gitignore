# Metrics data is never committed — generated at publish time (codemetrics get → build.py)
# and injected into the Pages artifact only (see .github/workflows/deploy.yml).
raw.json
data/raw.json
data/meta.json
data/charts/
node_modules/
bundle.js
og.png

# SwiftPM build artifacts for the data build tool (Tools/DashboardBuild)
.build/
Tools/DashboardBuild/.build/
Package.resolved
